using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MyEnums
{
    public enum VerticalDirection { DOWN = -1, CENTER = 0, UP = 1 }
    public enum HorizontalDirection { LEFT = -1, CENTER = 0, RIGHT = 1 }
    public enum CollisionType { UP, DOWN, SIDE };
}
